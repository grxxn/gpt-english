import React from 'react';
import styled from 'styled-components';

const Main = () => {
  return (
    <MainContainer>
      
    </MainContainer>
  );
};

const MainContainer = styled.div`

`;

export default Main;